# Binder configuration file
# Specifies which classes and functions to bind

# Include the main header
+include <mylib.hpp>

# Bind everything in the global namespace
+namespace ::

# Explicitly include these classes
+class Array1D

# Explicitly include these functions
+function initialize_kokkos
+function is_kokkos_initialized
+function dot_product

# Exclude Kokkos internals (they have their own bindings or shouldn't be exposed)
-namespace Kokkos
-class Kokkos::View
-class Kokkos::DualView
